'use strict';
window.addEventListener('load', function () {
    midLineAnimation();
    imgChange();
    slider();
    naviScrollAnimation();
    mobileClickEvent();
    window.addEventListener('scroll', scrollIndicator);
});
