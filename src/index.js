'use strict';
window.addEventListener('load', function () {
    console.log('아');
    midLineAnimation();
    imgChange();
    slider();
    naviScrollAnimation();
    mobileClickEvent();
    window.addEventListener('scroll', scrollIndicator);
});
