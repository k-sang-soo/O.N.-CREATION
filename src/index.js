'use strict';
window.addEventListener('load', () => {
    
    midLineAnimation()
    imgChange();
    slider();
    naviScrollAnimation();
    mobileClickEvent();
    window.addEventListener('scroll', scrollIndicator);
    
});